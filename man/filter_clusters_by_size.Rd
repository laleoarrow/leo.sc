% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotation.R
\name{filter_clusters_by_size}
\alias{filter_clusters_by_size}
\title{Filter clusters by size (proportion or absolute count)}
\usage{
filter_clusters_by_size(
  seurat_obj,
  cluster_col,
  prop_threshold = 0.001,
  abs_threshold = NULL,
  verbose = TRUE
)
}
\arguments{
\item{seurat_obj}{A Seurat object.}

\item{cluster_col}{Character. Metadata column with cluster labels.}

\item{prop_threshold}{Numeric. Minimum fraction of total cells (e.g., 0.001 = 0.1\%).
Ignored if \code{abs_threshold} is supplied. Default: 0.001.}

\item{abs_threshold}{Numeric. Minimum absolute cell count. Overrides
\code{prop_threshold} if not \code{NULL}. Default: \code{NULL}.}

\item{verbose}{Logical. Whether to print \code{leo.log} messages. Default: TRUE.}
}
\value{
A character vector of cluster labels to keep.
}
\description{
Reports cluster counts (descending) then returns the labels of clusters meeting
either a minimum fraction of total cells or a user‐provided absolute cell count.
}
\examples{
# Keep clusters comprising ≥0.5\% of cells
keep_pct <- filter_clusters_by_size(all,
                                    "harmony_clusters",
                                    prop_threshold = 0.005)
# Keep clusters with at least 100 cells
keep_abs <- filter_clusters_by_size(all,
                                    "harmony_clusters",
                                    abs_threshold = 100)
}
\seealso{
\code{\link{get_cluster_counts}}
}
