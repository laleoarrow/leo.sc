% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/celltype.R
\name{leo.augur}
\alias{leo.augur}
\title{Run Augur analysis}
\usage{
leo.augur(
  srt,
  subset_col = NULL,
  subset_value = c("Control", "Inactive"),
  label_level = NULL,
  label_col = "Stage1",
  cell_type_col = "cell_anno",
  n_threads = 8,
  return = "plot"
)
}
\arguments{
\item{srt}{Seurat object}

\item{subset_col}{Character. Column in meta.data to subset by (e.g. "Stage1")}

\item{subset_value}{Character vector. Values to retain in \code{subset_col}}

\item{label_level}{Character vector. Factor level order for \code{subset_col} (default: same as \code{subset_value})}

\item{label_col}{Character. Column in meta.data used as label for Augur}

\item{cell_type_col}{Character. Column in meta.data representing cell types}

\item{n_threads}{Integer. Number of threads for parallel Augur computation}

\item{return}{Character. If \code{"plot"} (default), returns a list of plot and data; otherwise returns raw Augur object}
}
\value{
If \code{return = "plot"}, a list with \code{plot} (ggplot object) and \code{dat} (Augur result); else, Augur result only
}
\description{
We added functionality to subset srt obj so that you can compare differet group's results
}
