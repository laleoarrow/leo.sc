% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/basic.R
\name{doublet_removal}
\alias{doublet_removal}
\title{Remove doublets using DoubletFinder}
\usage{
doublet_removal(
  seurat_obj = sc_matrix,
  out_path,
  pN = 0.25,
  PCs = 1:20,
  nPCs_for_pK = 1:10,
  doublet_rate = NULL,
  sct = FALSE,
  slim = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{seurat_obj}{Seurat object (after normalization & PCA).}

\item{out_path}{Character. Directory to save UMAP plots. Default: "."}

\item{pN}{Numeric. Proportion of artificial doublets. Default: 0.25.}

\item{PCs}{Integer vector. PCs to use. Default: 1:20.}

\item{nPCs_for_pK}{Integer vector. PCs to use for pK estimation. Default: 1:10.}

\item{doublet_rate}{Numeric. Expected doublet rate (0–1). If NULL, looked up via \code{doublet_rate_dictionary}.}

\item{sct}{Logical. Whether data is SCTransformed. Default: FALSE.}

\item{slim}{Logical. Whether to slim the Seurat object to only leave RNA count. Default: TRUE.}

\item{verbose}{Logical. Whether to print \code{leo.log} messages. Default: TRUE.}
}
\value{
Seurat object with doublets removed.
}
\description{
Estimates doublet rate (if not provided), finds optimal pK, and removes
heterotypic doublets from a Seurat object.
}
\note{
\itemize{
\item PCs: a vector of statistically significant PCs to use
\item pN: the number of artificially generated doublets (default = 0.25); robust, normally do not need fine-tune
\item pK: PC neighborhood size used to compute network; need fine-tune
\item nExp: threshold used to make doublet/singlet call; based on empirical ratios
}
}
